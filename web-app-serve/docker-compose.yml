# NOTE: The name is mandatory and should be unique
name: ${COMPOSE_PROJECT_NAME:-ifrcgo-montandon-etl-dashboard}

services:
  web-app-serve:
    build:
      context: ../
      target: web-app-serve
    environment:
<<<<<<< HEAD
      # web-app-serve config: dumps unreplaced placeholders + diffs at /__debug/
      APPLY_CONFIG__ENABLE_DEBUG: "true"
      # NOTE: APP_TITLE is intentionally absent — it is a default (overridable)
      # var baked as an ENV ("Montandon ETL Dashboard") in the Dockerfile final
      # stage. Passing it here (even as ${APP_TITLE:-...}) would mask the
      # baked-default test. Override it at runtime by setting APP_TITLE in the
      # environment / local .env.
      # Dynamic configs (local defaults; override via an optional local .env)
      APP_GRAPHQL_ENDPOINT: "${APP_GRAPHQL_ENDPOINT:-http://localhost:8000/graphql/}"
    env_file:
      - path: .env
        required: false
||||||| parent of b657363 (feat(web-app-serve): update web-app-serve)
      APP_TITLE: ${APP_TITLE:- Montandon ETL Dashboard}
      APP_GRAPHQL_ENDPOINT: ${APP_GRAPHQL_ENDPOINT:-http://localhost:8000/graphql/}
      APPLY_CONFIG__ENABLE_DEBUG: true
=======
      APP_TITLE: ${APP_TITLE:- Montandon ETL Dashboard}
      APP_GRAPHQL_ENDPOINT: ${APP_GRAPHQL_ENDPOINT:-http://localhost:8000/graphql/}
      # web-app-serve config
      APPLY_CONFIG__ENABLE_DEBUG: true
    env_file: .env
>>>>>>> b657363 (feat(web-app-serve): update web-app-serve)
    ports:
      - '8050:80'
